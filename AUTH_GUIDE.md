# คู่มือการใช้งานระบบ Authentication

## 🔐 ภาพรวมระบบ Authentication  

ระบบ POS ได้รับการอัพเกรดด้วยระบบ Authentication ที่สมบูรณ์แล้ว รองรับการแบ่งสิทธิ์ผู้ใช้เป็น 2 ระดับ:

- **User (ผู้ใช้ทั่วไป)** - เข้าถึงหน้า POS Storefront (จุดขาย) เท่านั้น
- **Admin (ผู้ดูแลระบบ)** - เข้าถึงได้ทุกหน้า รวมถึงหน้าแดชบอร์ดและการจัดการสินค้า

---

## 📝 ขั้นตอนการเริ่มต้นใช้งาน

### 1. รีสตาร์ทระบบด้วยข้อมูลใหม่

เนื่องจากมีการเพิ่มตาราง users ลงในฐานข้อมูล คุณต้องรีสตาร์ทระบบเพื่อสร้างตารางใหม่:

```powershell
# หยุดระบบ และลบข้อมูลฐานข้อมูลเดิม
docker-compose down -v

# สร้างและเริ่มระบบใหม่
docker-compose up --build
```

**หมายเหตุ**: `-v` จะลบ volumes ทั้งหมด รวมถึงข้อมูลฐานข้อมูล การทำเช่นนี้จะสร้างตาราง users พร้อมบัญชีทดสอบใหม่

### 2. รอให้ระบบเตรียมพร้อม

รอประมาณ 1-2 นาทีให้:
- MySQL สร้างตารางทั้งหมด (รวมตาราง users ใหม่)
- Backend เชื่อมต่อกับฐานข้อมูล
- Frontend compile เสร็จสิ้น

---

## 👤 บัญชีทดสอบ

ระบบมีบัญชีทดสอบพร้อมใช้งาน 5 บัญชี:

### บัญชี Admin (ผู้ดูแลระบบ)

#### 1. บัญชี Admin หลัก
- **Username**: `admin`
- **Password**: `password123`
- **สิทธิ์**: เข้าถึงได้ทุกหน้า (POS, Dashboard, Product Management)

#### 2. บัญชี Manager
- **Username**: `manager`
- **Password**: `password123`
- **สิทธิ์**: เข้าถึงได้ทุกหน้า

### บัญชี User (ผู้ใช้ทั่วไป)

#### 3. Cashier 1
- **Username**: `cashier1`
- **Password**: `password123`
- **สิทธิ์**: เข้าถึงหน้า POS เท่านั้น

#### 4. Cashier 2
- **Username**: `cashier2`
- **Password**: `password123`
- **สิทธิ์**: เข้าถึงหน้า POS เท่านั้น

#### 5. User ทั่วไป
- **Username**: `user`
- **Password**: `password123`
- **สิทธิ์**: เข้าถึงหน้า POS เท่านั้น

---

## 🚀 การเข้าสู่ระบบ

### หน้า Login

1. เปิดเบราว์เซอร์และไปที่: **http://localhost:3000**
2. ระบบจะนำคุณไปยังหน้า Login โดยอัตโนมัติ
3. กรอก Username และ Password
4. คลิก **"เข้าสู่ระบบ"**

### ปุ่ม Demo Credentials (ทดสอบง่าย)

ในหน้า Login มีปุ่มทดสอบให้คลิกเพื่อกรอกข้อมูลอัตโนมัติ:
- **👨‍💼 Admin Demo** - กรอกข้อมูล admin ให้อัตโนมัติ
- **👤 User Demo** - กรอกข้อมูล cashier1 ให้อัตโนมัติ

---

## 🎯 สิทธิ์การเข้าถึง

### สิทธิ์ User (ผู้ใช้ทั่วไป)
✅ เข้าถึง:
- หน้า POS Storefront (/)

❌ ไม่สามารถเข้าถึง:
- หน้า Dashboard (/admin)
- หน้าจัดการสินค้า (/admin/products)

หากพยายามเข้าถึงหน้าที่ไม่มีสิทธิ์ จะแสดงข้อความ **"ไม่มีสิทธิ์เข้าถึง"**

### สิทธิ์ Admin (ผู้ดูแลระบบ)
✅ เข้าถึงได้ทุกหน้า:
- หน้า POS Storefront (/)
- หน้า Dashboard (/admin)
- หน้าจัดการสินค้า (/admin/products)
- สร้าง/แก้ไข/ลบสินค้า
- จัดการผู้ใช้ (ผ่าน API)

---

## 📱 การใช้งานหลังเข้าสู่ระบบ

### Navbar (แถบเมนู)

เมื่อเข้าสู่ระบบสำเร็จ จะเห็น Navbar ด้านบนแสดง:
- **ชื่อผู้ใช้** และ **Badge Admin** (ถ้าเป็น Admin)
- **ปุ่มออกจากระบบ** (Logout)

#### สำหรับ User:
```
POS System | Storefront | [ชื่อผู้ใช้] | Logout
```

#### สำหรับ Admin:
```
POS System | Storefront | Dashboard | Products | [ชื่อผู้ใช้ Admin] | Logout
```

### การออกจากระบบ

คลิกปุ่ม **"Logout"** / **"ออกจากระบบ"** ที่มุมขวาบน

---

## 📋 การลงทะเบียนผู้ใช้ใหม่

### ขั้นตอนการลงทะเบียน

1. ที่หน้า Login คลิก **"ลงทะเบียนที่นี่"**
2. กรอกข้อมูล:
   - ชื่อผู้ใช้ (Username) *
   - อีเมล (Email) *
   - ชื่อ-นามสกุล (Full Name)
   - รหัสผ่าน (อย่างน้อย 6 ตัวอักษร) *
   - ยืนยันรหัสผ่าน *
3. คลิก **"ลงทะเบียน"**

**หมายเหตุ**: ผู้ใช้ที่ลงทะเบียนเองจะได้สิทธิ์เป็น **User** เท่านั้น (ไม่ใช่ Admin)

---

## 🔧 การจัดการผู้ใช้ (Admin)

Admin สามารถจัดการผู้ใช้ผ่าน API ได้:

### ดูรายชื่อผู้ใช้ทั้งหมด

```bash
curl http://localhost:5000/api/auth/users \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### สร้างผู้ใช้ใหม่ (Admin เท่านั้น)

```bash
curl -X POST http://localhost:5000/api/auth/users \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "username": "newuser",
    "email": "newuser@example.com",
    "password": "password123",
    "full_name": "New User",
    "role": "user"
  }'
```

### อัพเดทข้อมูลผู้ใช้

```bash
curl -X PUT http://localhost:5000/api/auth/users/1 \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "full_name": "Updated Name",
    "role": "admin"
  }'
```

### ลบผู้ใช้

```bash
curl -X DELETE http://localhost:5000/api/auth/users/5 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

**หมายเหตุ**: ไม่สามารถลบบัญชีตัวเอง

---

## 🔑 การเปลี่ยนรหัสผ่าน

### ผ่าน API

```bash
curl -X PUT http://localhost:5000/api/auth/change-password \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "currentPassword": "password123",
    "newPassword": "newpassword456"
  }'
```

---

## 🔐 JWT Token

### การทำงานของ Token

1. เมื่อ Login สำเร็จ ระบบจะออก JWT Token
2. Token จะถูกเก็บใน `localStorage`
3. ทุก API request จะส่ง Token ไปใน Header: `Authorization: Bearer TOKEN`
4. Token มีอายุ **7 วัน** (ตั้งค่าได้ใน `.env`)

### Token Expiration

เมื่อ Token หมดอายุ:
- ระบบจะ redirect ไปหน้า Login อัตโนมัติ
- ต้อง Login ใหม่เพื่อรับ Token ใหม่

---

## 📱 API Endpoints สำหรับ Authentication

### Public Endpoints (ไม่ต้อง Login)

```
POST /api/auth/register    - ลงทะเบียนผู้ใช้ใหม่
POST /api/auth/login       - เข้าสู่ระบบ
```

### Protected Endpoints (ต้อง Login)

```
GET  /api/auth/profile           - ดูข้อมูลโปรไฟล์
PUT  /api/auth/profile           - แก้ไขโปรไฟล์
PUT  /api/auth/change-password   - เปลี่ยนรหัสผ่าน
```

### Admin Only Endpoints

```
GET    /api/auth/users      - ดูรายชื่อผู้ใช้ทั้งหมด
POST   /api/auth/users      - สร้างผู้ใช้ใหม่
PUT    /api/auth/users/:id  - แก้ไขข้อมูลผู้ใช้
DELETE /api/auth/users/:id  - ลบผู้ใช้
```

---

## 🗃️ Database Schema - Users Table

```sql
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    full_name VARCHAR(100),
    role ENUM('user', 'admin') DEFAULT 'user',
    is_active BOOLEAN DEFAULT TRUE,
    last_login TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

---

## ⚙️ การตั้งค่า Environment Variables

### Backend (.env)

```env
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
```

### การเปลี่ยน JWT Secret (สำคัญ!)

ใน Production ต้องเปลี่ยน `JWT_SECRET` เป็นค่าที่ปลอดภัย:

```bash
# สร้าง random secret key
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

จากนั้นนำค่าที่ได้ไปใส่ใน `.env`

---

## 🧪 การทดสอบระบบ

### ทดสอบด้วย User Account

1. Login ด้วย `cashier1` / `password123`
2. ตรวจสอบว่าเห็นเฉพาะหน้า POS
3. พยายามเข้า http://localhost:3000/admin โดยตรง
4. ควรเห็นหน้า "ไม่มีสิทธิ์เข้าถึง"

### ทดสอบด้วย Admin Account

1. Login ด้วย `admin` / `password123`
2. ตรวจสอบว่าเห็น Badge "Admin" ที่ navbar
3. ตรวจสอบว่าเห็นเมนู Dashboard และ Products
4. ทดลองสร้าง/แก้ไข/ลบสินค้า

### ทดสอบ Token Expiration

1. Login เข้าระบบ
2. เปิด DevTools > Application > Local Storage
3. ลบ `token` ออก
4. Refresh หน้าเว็บ
5. ควร redirect ไปหน้า Login

---

## 🚨 Troubleshooting

### ปัญหา: ไม่สามารถ Login ได้

**แก้ไข:**
1. ตรวจสอบว่า Backend รันอยู่: http://localhost:5000/api/health
2. ตรวจสอบ Console browser สำหรับ error
3. ตรวจสอบว่าฐานข้อมูลมีตาราง users
   ```bash
   docker exec -it pos_mysql mysql -u root -prootpassword -e "USE pos_db; SHOW TABLES;"
   ```

### ปัญหา: Token หมดอายุบ่อยเกินไป

**แก้ไข:** เพิ่มเวลาใน `.env`
```env
JWT_EXPIRES_IN=30d   # เปลี่ยนเป็น 30 วัน
```

### ปัญหา: ตาราง users ไม่มีในฐานข้อมูล

**แก้ไข:** รีสร้างฐานข้อมูล
```bash
docker-compose down -v
docker-compose up --build
```

### ปัญหา: CORS Error

**แก้ไข:** ตรวจสอบว่า backend มี CORS config:
```javascript
app.use(cors());
```

---

## 🎨 UI Features

### หน้า Login/Register
- ✅ Responsive design
- ✅ Gradient background
- ✅ ใส่ข้อมูล Demo ได้ง่ายด้วยปุ่ม
- ✅ Error messages แบบ realtime
- ✅ Password validation

### Navbar
- ✅ แสดงชื่อผู้ใช้และสถานะ Admin
- ✅ เมนูแตกต่างตาม role
- ✅ ปุ่ม Logout พร้อมสไตล์

### Protected Routes
- ✅ Loading state ขณะตรวจสอบสิทธิ์
- ✅ หน้า Access Denied สำหรับผู้ใช้ที่ไม่มีสิทธิ์
- ✅ Auto redirect ไปหน้า Login เมื่อไม่มีผู้login

---

## 📚 เอกสารเพิ่มเติม

- [README.md](README.md) - คู่มือหลักของโปรเจค
- [API_DOCUMENTATION.md](API_DOCUMENTATION.md) - API Documentation ฉบับสมบูรณ์

---

## ✨ สรุป

ระบบ Authentication พร้อมใช้งาน! คุณสามารถ:

✅ เข้าสู่ระบบด้วยบัญชีทดสอบ 5 บัญชี
✅ ลงทะเบียนผู้ใช้ใหม่
✅ แบ่งสิทธิ์ระหว่าง User และ Admin
✅ ป้องกันหน้าที่ต้องการสิทธิ์พิเศษ
✅ จัดการ Token อัตโนมัติ

**Happy Coding! 🎉**
